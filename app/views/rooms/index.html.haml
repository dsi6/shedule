%h1 Список аудиторий
%table.bordered-table
  %thead
    %tr
      %th Ном. корп.
      %th Ном. ауд.
      %th Вместимость
      %th Вид. ауд.
      -if @current_user && @current_user.admin?
        %th
        %th
        %th
  %tbody
    - @rooms.each do |room|
      %tr
        %td= room.rname
        %td= room.audience
        %td= room.rcount_aud
        %td= room.comment
        -if @current_user && @current_user.admin?
          %td= link_to 'Посмотреть', room
          %td= link_to 'Редактировать', edit_room_path(room)
          %td= link_to 'Удалить', room, :confirm => 'Are you sure?', :method => :delete

-if @current_user && @current_user.admin?
  .actions
    = link_to 'Новая аудитория', new_room_path, :class=>"btn primary"
