= form_for @semester do |f|
  -if @semester.errors.any?
    .alert-message
      %h2= "при сохранении возникли ошибки:"
      %ul
        - @semester.er.each do |msg|
          %li= msg

  .field
    = f.label :semnum
    %br
    = f.radio_button :semnum,"1",:checked=>@semester.semnum?
    Осенний семестр
    = f.radio_button :semnum,"0",:checked=>!@semester.semnum?
    Весенний семестр
  .field
    = f.label :wcount
    = f.number_field :wcount, :size=>1
  .field
    = f.label :stsem
    %br
    = f.text_field :stsem
  .field
    = f.label :finsem
    %br
    = f.text_field :finsem
  .actions
    = f.submit 'Сохранить'
