%h1 Создание потока

= form_for @flow do |f|
  -unless @flow.blank?
    -if @flow.errors.any?
      .alert-message
        %h2= "при сохранении возникли ошибки:"
        %ul
          - @flow.errors.full_messages.each do |msg|
            %li= msg

  .field
    =f.label :group_tokens, "Группы"
    %br
    =f.text_field :group_tokens,"data-pre"=>@flow.groups.map(&:attributes).to_json
    %br
  .field
    = f.label :comment
    = f.text_field :comment
  .actions
    = f.submit 'Сохранить'
